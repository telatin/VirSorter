language: perl
addons:
  apt:
    packages:
    - build-essential
    - bzip2
    - wget
sudo: false
install:
  - "source ./t/install.sh"
before_script:
  - cpanm --quiet FASTX::Reader
script: "perl ./t/test.pl"

